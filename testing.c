//trying a single line exercise

#include <stdio.h>

int main(){
	int m1,m2,m3;
	m1=92;
	m2=82;
	m3=72;
	puts(m1>=90 ? "A": m1>=80 ? "B": m1>=70 ? "C": "D");
	puts(m2>=90 ? "A": m2>=80 ? "B": m2>=70 ? "C": "D");
	puts(m3>=90 ? "A": m3>=80 ? "B": m3>=70 ? "C": "D");
	return 0;
}
